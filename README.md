# Vestara API

Standalone HTTP + WebSocket API gateway for the Vestara platform.

**Status: Phase 0 — baseline snapshot complete.** This repository is created and
pinned as a gitlink in the vestara root. The API implementation is currently
mirrored in `vestara-ai-core/apps/api`; it moves here per
`thoughts/plans/vestara-api-restructure.md` (Phase 2+). The current API
continues to run from its original location until cutover.

See the restructure plan for the phased execution and integration checklist.
